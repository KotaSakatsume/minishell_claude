# 05-pr.md

- **Issue:** #2 プロジェクト基盤(最小 REPL スケルトン)
- **Stage:** 5/5 Integrator
- **Branch:** `feat/issue-2-foundation` → `main`
- **差し戻し往復:** 0 回(段階4で must 級ゼロ)

## PR タイトル
`[pipeline] feat: project foundation — Makefile + readline REPL skeleton (Closes #2)`

## PR 本文(GitHub 投稿用)
下記 `## PR Body` セクションがそのまま PR 本文になる。`Closes #2` を含む。

---

## PR Body

### Summary
42 Minishell の最小実行可能スケルトン(Issue #2)。後続の全実装(レキサ/パーサ/実行/組み込み/シグナル)が乗る土台を用意する。

- **Makefile**: `-Wall -Wextra -Werror` / `cc` / `all clean fclean re $(NAME)` / 不要再リンク回避 / `brew --prefix readline` による readline 動的パス解決 / libft を `libft/libft.a` ターゲットで sub-make 先行ビルド
- **`t_shell` 状態構造体**をポインタ渡し。**グローバル変数 0 個**(唯一許可されるグローバルは将来のシグナル処理用に温存)
- **REPL ループ**: プロンプト表示 / history(`add_history`)/ ctrl-D(EOF)で `exit` 出力して終了 / 空行・空白のみはスキップ / 各反復で `free(line)`
- **`process_line()`**: 将来のレキサ/パーサ/実行の単一接続点(現状は libft `ft_strlen` 経由でエコー)
- 最小 libft(`ft_strlen`)を `libft.a` として結線

### Verification(すべて実測 / Intel Mac・Apple clang 14・GNU Make 3.81)
- ✅ `make` 警告ゼロビルド、二度目 `make` で再ビルドなし、ヘッダ変更で再ビルド
- ✅ `make clean/fclean/re/all` 機能、libft 連動
- ✅ `otool -L` が Homebrew `libreadline.8.dylib` を掴む(libedit 取り違えなし)
- ✅ エコー / EOF→`exit` / 空行スキップ、クラッシュなし(非対話スモーク)
- ✅ 自作コード **0 leaks**(`leaks --atExit`)
- ✅ norminette: 42 ヘッダー以外の違反ゼロ(関数25行 / 5関数 / while のみ / グローバル0個 / 80列)

### Known / Deferred
- **42 ヘッダー未付与**: norminette が全ファイルで `INVALID_HEADER`。42 ログイン確定待ちのため**意図的に保留**。ログイン確定後に一括付与で解消。
- **should(別Issue)**: (1) 非対話モードで prompt/`exit` が出る → `isatty()` ガードはシグナル/実行Issueで対応 (2) `shell_cleanup` の dead assignment 削減。

### Scope(やらないこと)
レキサ/パーサ/実行(execve/fork/pipe/redirect)/組み込み/シグナル/環境変数展開/引用符/heredoc は本PRの対象外(別Issue)。

### Pipeline artifacts
`docs/pipeline/issue-2/` に 01-design / 02-research / 03-implementation / 04-review / 05-pr を同梱。各段階の成果物は Issue #2 のコメントにも転記済み。

Closes #2

---

## Integrator 実行ログ
- push: `feat/issue-2-foundation` → `origin`(新規ブランチ、コミット d86a9e0 + a27f7ae)
- PR URL: https://github.com/KotaSakatsume/minishell_claude/pull/3(本文に `Closes #2`)
